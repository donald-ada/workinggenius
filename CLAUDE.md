# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

The Working Genius plugin itself: a Claude Code plugin — 22 skills under `skills/` and three subagents under `agents/`, distributed through the plugin marketplace (`.claude-plugin/`). The skills are prose, plus three things only Claude Code reads that ride inside the same folders (below) and one Claude Mod at the root (`hooks/`, the same two judges as a function hook, below); nothing here is held back for another client (user ruling, 2026-09-05). There is no runtime and no build; the one script is the format's instrument, `skills/genius-file/measure.py`, which counts and never decides — a fork must be able to carry the whole thing as plain folders, and still can, because the module is additive and the classic hooks stay. The one test suite is the module's kit (`tests/`), run by Claude Code itself. Dogfood work files (`.genius/`) are gitignored and never shipped.

## Commands

There is nothing to build or run. Four checks matter when editing skills, and a fifth when editing the module:

- **Count characters, never bytes.** `python3 skills/genius-file/measure.py count <file>` is the instrument and this one-liner is what it does; any number that enters a skill or a record comes from one of these and not from `wc -m` (which counts bytes under most non-interactive locales) or `awk length()`:
  ```
  python3 -c "import sys;print(len(open(sys.argv[1],encoding='utf-8').read()))" <file>
  ```
- **Relative links between skill files must resolve** (`agents/inventor.md`, `../tenacity/agents/reviewer.md`, `FILE-FORMAT.md`, `../genius-file/BACKLOG-FORMAT.md`). A one-off check:
  ```
  python3 -c "import re,os,glob;print([(f,t) for f in glob.glob('skills/**/*.md',recursive=True) for t in re.findall(r'\]\(([^)#]+)(?:#[^)]*)?\)',open(f,encoding='utf-8').read()) if not t.startswith('http') and '<' not in t and t!='CONTRACT.md' and not t.endswith('.log.md') and not os.path.exists(os.path.join(os.path.dirname(f),t))])"
  ```
- **Frontmatter must parse, and the instrument must not fail.** The preloads and the judge's command hooks live in YAML frontmatter, and a stray indent drops the whole block without a word; `measure.py` is injected into four commands and a skill whose injected command fails is aborted whole, so it exits 0 on an empty directory and on a malformed snapshot; `stop-judge.py` blocks a stop only on a clear verdict, so it exits 0 on garbage and on an absent `claude`. One check for all three:
  ```
  python3 -c "import yaml,glob;[yaml.safe_load(open(f,encoding='utf-8').read().split('\n---\n')[0][4:]) for f in glob.glob('skills/*/SKILL.md')+glob.glob('skills/*/agents/*.md')];print('frontmatter ok')" && python3 skills/genius-file/measure.py status >/dev/null && printf 'x' | python3 skills/genius-file/stop-judge.py && echo 'instruments ok'
  ```
- **The module must validate, typecheck and pass its kit.** `hooks/register.ts` is loaded by the engine itself, so `claude plugin validate .claude-plugin/plugin.json` (pointed at the plugin manifest: `.` picks the marketplace manifest beside it and never reads the module) lists what the module hooks and calls and refuses what the engine would refuse — the validator tracks every use of `$` statically, so `$` passed to anything but a top-level function declaration is refused. `tsc -p tsconfig.json` checks it against `types/claude-code.d.ts`, which `/plugin-types` writes (`claude -p "/plugin-types"` works headless) and which is regenerated on each Claude Code release, its first line naming the version. `--strict` fails on one pre-existing warning, this file at the root, which is not plugin context. The three together:
  ```
  claude plugin validate .claude-plugin/plugin.json && tsc -p tsconfig.json && CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1 claude plugin test .
  ```
- **The skills have an eval suite**, `evals/`, run by `claude plugin eval` (2.1.269 and later): one case per claim the skills make that a fresh session can be caught breaking — an ordinary request never enters the flow (`no-hijack`), a question about work in flight is answered from the instrument and invents nothing (`nothing-in-flight`), an idea handed to the flow is interviewed and not built (`genius-starts-wonder`), and one slice is built red before green with its evidence in the log (`enable-red-before-green`, scaffolded). Each case is a prompt as a person would type it plus graders that cost nothing (`tool_used`, `tool_order`, `regex`, `file_exists`) and one judge rubric; results land in `evals/results/`, gitignored. Two things measured about the runner, 2026-09-23: a prompt that is a slash command (`/genius`, `/enable demo, slice 2`) is not run — zero turns, no error — so the user-typed commands are outside an eval's reach and the cases name the model-invoked skills by what a person would say; and a case that grants `Bash`, `Write` or `Edit` needs the runner's sandbox (`bubblewrap` and `socat` on Linux), refused otherwise. The smoke case runs on haiku and the flow cases on sonnet, because haiku did not fire `genius-file` on the in-flight question at all (measured), which is a fact about triggering on a small model and not a reason to loosen the case:
  ```
  claude plugin eval . --tag smoke --runs 1 --ablation none --model haiku --allow-tools Bash Write Edit --trust-plugin --no-publish
  claude plugin eval . --tag flow --runs 1 --ablation none --model sonnet --scaffold --allow-tools Bash Write Edit --trust-plugin --no-publish --max-cost-usd 10
  ```
  The first scores, 2026-09-23: `no-hijack` 1.0, `nothing-in-flight` 1.0, `genius-starts-wonder` 1.0, `enable-red-before-green` 0.57 — the test written and run red before the implementation was edited and the suite run twice, but no `## slice-2` entry in the log, the roster's S2 unchecked and the hand-back without per-criterion evidence: the build discipline held and the close did not land, the first finding the suite produced (`MODS-DESIGN.md` records it and its reproduction by hand).
- **Release.** Any change under `skills/` or `.claude-plugin/` that merges to `main` bumps `version` in `.claude-plugin/plugin.json` in the same change — marketplace clients detect updates only through that number. A skill added or removed moves the count in README's first line.

## Architecture

**One skill = one folder** `skills/<name>/SKILL.md`, frontmatter `name`, `description`, optional `argument-hint`. `disable-model-invocation: true` marks a command only the user types (`/genius`, `/architect`, `/designer`, `/compact`, `/distill`, `/reconcile`, `/triage`, `/waitwhat`, `/setup-working-genius`); the six stage skills and the discipline skills (`genius-file`, `record-prose`, `errata`, `decision-record`, `domain-glossary`, `blindspot`, `diagnose`) are model-invoked. **The three subagents** — `skills/invent/agents/inventor.md` (spawned by `/invent`, one per angle), `skills/enable/agents/builder.md` (by `/enable`'s coordinator, one per slice), `skills/tenacity/agents/reviewer.md` (by `/enable` at a slice close and `/tenacity` at close-out) — are Claude Code plugin agents, declared by path in `plugin.json`'s `agents` field: frontmatter `name` and `description`, and no `tools` list, because the agent inherits everything the session has and a list only names less (the reviewer's read-only discipline is a rule in its body, not a grant; `Bash` could write anyway). The body is the subagent's system prompt and carries the discipline, while the spawning skill's task message carries the parameters (the problem and angle, the slice and where to build, the diff and what it is judged against). They live inside the skill that spawns them, not in a top-level `agents/`, so a skill, its task message and its agent's brief change in one folder. The body still stands alone as a brief, because the preload (below) is context and not a guarantee. **Measured, 2026-09-23, 2.1.280:** a builder spawned by the Agent tool expands all three of its preloads through `skill.prompt` (`workinggenius:genius-file`, `record-prose`, `decision-record`), so the preload does reach a spawned agent, and no `agent.spawn` hook re-attaches it. The skill names what the message must carry; the agent names what it hands back — one home each.

**Three things only Claude Code reads**, each inside the folder of the skill that uses it: `/genius`, `/compact`, `/reconcile` and `/distill` inject the instrument's output before the model reads — `status`, `snapshots`, `links`, `distill` — and their `allowed-tools` pre-approves exactly that command, because an injected command that would prompt aborts the invocation (`anchors <slug>` is run by the session at a close instead, pre-approved in `/enable` and `/tenacity`, because an injection is taken at invocation and a close comes later); the three agents preload the discipline skills by scoped name (`workinggenius:genius-file`, …), so the skills are the home and the body restates only what a brief must; and two judges, one `command`-type hook, `skills/genius-file/stop-judge.py`, registered by `/enable` (`Stop` and `SubagentStop` matched on the builder) and `/tenacity` (`Stop`) — by invocation, never by installation, because a plugin-level `hooks/hooks.json` fires in every session with the plugin enabled, flow or not, and the flow never hijacks a request that didn't enter it. The judge blocks only a turn announcing its own next dispatch with nothing running in the background, or a builder's claim of done with no `command → result` line; three free checks settle most stops (`stop_hook_active`, a running subagent, no work at enablement or tenacity — the one frontmatter field it reads), and only then a fast model reads the turn's last message, never the transcript, through a nested `claude -p` with hooks, skills, tools and MCP off; every failure of its own allows the stop. Claude Code ignores, with a warning, hooks written in a plugin agent's own frontmatter, so the builder's judge lives with the skill that spawns it.

**The same two judges run as a Claude Mod where function hooks are on** — `hooks/hooks.json` names `hooks/register.ts`; early access, loaded only under `CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1`, and an API that may change between releases without notice, which is why `types/claude-code.d.ts` is committed with the version that wrote it on its first line and `MODS-DESIGN.md` records what was measured against which. The module is installed with the plugin and loads in every session, so it is **armed** only once `skill.prompt` sees `workinggenius:enable` or `workinggenius:tenacity` in that session — installed by the module, armed by invocation, the frontmatter's rule kept — and until then every `classic.Stop` and `classic.SubagentStop` is `next(e)` straight through. Armed, it makes the command hook's three free checks (`stop_hook_active`; a subagent or workflow among `background_tasks`; no work at enablement or tenacity, read through `measure.py status` over `$.process.run` and never a parser of its own), then one `$.model.complete` on haiku with the condition as the system prompt and the turn's last message alone, and answers `{}` or `{ block }` **without `next`**, so the command hook beneath never judges the same stop twice; only the module's own failure falls through with `next(e)` to the classic judge. The conditions live in `skills/genius-file/judge-conditions.md`, one home read by `stop-judge.py` and the module alike. The classic hooks stay: for every session without the flag, and for managed Team/Enterprise machines, where the built-in `sec-default` continues `classic.*` and `skill.prompt` past the user tier and a user-installed module never sees them. **Measured, 2026-09-23, Claude Code 2.1.280, headless (`claude -p`), a project with one work at enablement:** a staged stall was blocked by the module with the coordinator's reason and no line from `stop-judge.py` (the hook `/enable`'s frontmatter registered did not run beneath the module's answer); the model continued and asked the user; the second Stop passed on `stop_hook_active`; the same stall in a session that typed neither command produced no judge line at all; `import.meta.url` is how the module finds the plugin root, since `CLAUDE_PLUGIN_ROOT` is not in its environment; and a skill's `!` injection is already applied by the time `skill.prompt` sees its text, which is why the instrument stays injected by the skills and the module never touches it. The module also hooks `session.compact`: where the instrument's status lists work in flight, it appends to the compaction's instructions the note the `genius-file` skill otherwise leaves to memory — keep the slug, the stage, the exact `next:`, the snapshot's and `CONTRACT.md`'s paths, and re-read the snapshot whole first — with the status output verbatim, so the summary carries the slugs and commands as written and nothing is parsed (measured: a `/compact` after one turn produced a summary ending in a "Working Genius Metadata" block with all of them). And it logs one line per preloaded discipline skill that expands, which is how the preload was measured; the log's writes queue behind one another, because three preloads expand at once and a read-modify-write of the file kept one line of three the first time. Two more things it does are display, never decision: a **status line** under the prompt (`$.ui.status`, one per plugin) reading `Working Genius · <slug> · <stage> · next: <command>` for one work in flight, a count pointing at `/genius` for several, nothing for none — pinned at `session.start` where work is in flight and refreshed at each `turn.complete`, but only once the flow was *entered* in the session (a `workinggenius:` skill expanded, or work in flight at the start), so a session that never entered it runs no instrument at its turns and shows no line; and after a `Write` or `Edit` that lands on a snapshot (the file named for its folder, `<slug>/<slug>.md`), the instrument's `snapshots` line for that slug as `context` beneath the tool result — the count at the moment of the action, which is where the format says the ceiling is checked (measured: it reaches the model as a `tool.call hook additional context` system-reminder attachment after the tool result, which `stream-json` does not show and the transcript does). The line is the instrument's own output re-framed, and the slug is read off the path's shape, so nothing parses a work file. The **map pane** is the same status drawn beside the transcript (`ui.render` of `Pane`, id `genius-map`): a block per work in flight (slug and stage, `next:`, the snapshot against the ceiling and the slices), then the done and backlog counts; `/genius-map`, a command the module registers at `session.start`, shows or hides it; it opens on its own where work is in flight, unless the person closed it (`$.store`, per plugin), and an open the surface cannot place yet is withdrawn so a later resize does not seat it unasked, as the built-in `/diff` does. It costs the model nothing: the pane is drawn by the engine from the instrument's output and never enters the context. Its drawing is proven by the kit's `$.ui.mount`; seeing it in a terminal is the interactive measurement still owed. **The checkpoint as a gesture.** The flow's oldest fear is a model answering its own interview; the `genius-file` skill's "checkpoints are live" was a rule with nothing to hold it. The module declares a tool, `mcp__workinggenius__confirm`, once the flow is entered (a `workinggenius:` skill expanded, or work in flight at the start; a session outside the flow never lists it), served by its own `tool.call` hook: it opens a pane with the statement and two buttons, and returns only when the person pressed *Yes, that's it* or *Not yet*, on Escape or a close, or after ten minutes with nobody, saying which; headless, with no surface to press on, it answers at once that nobody can press here and the yes must be asked in text. A press is a surface's act — the kit's `$.ui.press` is the only other way to make one — so the yes it returns is one a model cannot produce for the user, and the `wonder`, `discern` and `galvanize` skills take their confirmations through it where the session offers it, `genius-file` holding the why. Beside it, `prompt.submit`: once the flow is entered, a prompt whose origin is not the person's composer or bridge (a peer session, a coordinator, a schedule, a notification, an SDK host) carries one context line naming that origin, the engine's record of where the message came from; the skill says what it means, the module states the fact. Neither parses anything and neither decides. The kit (`tests/register.test.ts`, 29 cases) covers the block, the allow, the free checks, both failure paths, the unarmed pass-through, the compaction note, the status line, the write count, the pane's drawing and the command's toggle, the confirm tool's four endings and the origin note with the world beneath mocked. **Measured, 2026-09-05, all through a live two-turn session driven by the Agent SDK:** a skill's `prompt`-type `Stop` hook is removed the moment its condition is first met, so it guarded only the invoking turn — whatever the docs say about the rest of the session; a `command`-type hook registered by the same skill fired on both turns, which is why the judges are one; `${CLAUDE_PLUGIN_ROOT}` is substituted in a skill's frontmatter hook command and `${CLAUDE_SKILL_DIR}` is not; and `claude -p --bare` reads no OAuth login, so the nested call runs without `--bare`. A plugin-level `hooks/hooks.json` prompt hook also registers and persists (measured), and is not used, for the reason above. This reopens 275ed4f this far and no further (user ruling, 2026-09-05): what died there was a parser demanding gate blocks in work files; what returned is a model reading the same message a person would, at an action point, adding no line to any file. Three runs by hand are on record (2026-09-05 and 2026-09-06, a picking-robot warehouse scheduler built through the whole flow twice and a web spreadsheet once, the records in those commits' messages): 64 judge decisions — 46 stops allowed as pauses on background work, 14 model-judged allows, 4 allowed for no work in build — no block and no false block, and no stall for a judge to catch, so the runs show the judges cost nothing and prove nothing about the failure they name. `claude plugin eval` has opened (2.1.269) and `evals/` is its suite (Commands); the judges stay experiments, not a layer to extend, until a run catches the stall they name. The judge text is copied into `enable` and `tenacity` deliberately: a frontmatter cannot point.

**The flow** is six commands the user types in order — `/wonder → /invent → /discern → /galvanize → /enable → /tenacity` — with `/genius` as the map. The flow never advances itself and never hijacks a request that didn't enter it; a stage the user doesn't type has no section in the work file, and that absence is the record.

**The work file is the whole handoff between sessions**, and its design is the part that takes several files to see. One piece of work = `.genius/<slug>/` holding three files with three growth laws: the snapshot `<slug>.md` grows by *scope* and has a 6000-character ceiling (Slices roster excluded); the log `<slug>.log.md` grows by *time* and is append-only; `CONTRACT.md` grows by *slice count* and holds the brief, seams, pinned values, each slice's acceptance criteria (their one home — the snapshot's slice line only links them) and the conventions the build established. Compaction routes each snapshot line by one question — *does this line still constrain work that isn't finished?* — under one invariant: nothing leaves the snapshot except into the log or `CONTRACT.md`, with a link left behind. Cross-work files at `.genius/` (`BACKLOG.md` + `BACKLOG.log.md`, `DECIDED.md`, `HISTORY.md`) have their own format. Where this is written: `genius-file/FILE-FORMAT.md` (the shape), `genius-file/BACKLOG-FORMAT.md`, `genius-file/FORMAT-EDGES.md` (the measurements behind the rules, with commit hashes).

**Two layers run through every file the flow writes**: what *binds* (snapshot, contract, `DECIDED.md`, `CONTEXT.md`, backlog seeds) is rewritten in place when wrong; what *records* (any log, any done file) is appended to and never edited. `errata` owns that rule; `/reconcile` sweeps drift against the repo; `/compact` and `/distill` are the retroactive passes. There is one format, and no recognition of older ones: a file in another shape is read for what it holds and brought to the format when next written to.

**Each rule has exactly one home**, and other skills point at it rather than restate it — the compaction question and its destinations in FILE-FORMAT, the sentence discipline in `record-prose`, the correction moves in `errata`, the seed shape in BACKLOG-FORMAT. When editing, change the owning file and check the pointers; a rule copied into a second skill drifts the first time either is edited.

## How skills are written

- Concept first: each skill carries its purpose, its failure mode, the concept in one bold line, and one threshold that must be honestly true before the next stage. Stage skills also carry a short `## How it runs` list. Every rule states its why in the same sentence; a constraint that can't name its reason doesn't land.
- Action constraints are where the measured value lives and stay firm (interview is live dialogue, red before green, fresh verification, wounds found never manufactured, findings carry evidence). Taxonomies are floors, not scripts.
- Killed designs stay killed unless measured evidence reopens them. Before proposing express paths, sizing calls, autonomy modes, gate checklists in work files, hooks or scripts that parse a work file's prose or decide anything (the judge reads one frontmatter field to know whether to ask; the decision is the model's), skip bookkeeping, or old-format recognition, read the git history — `0cc21de`, `925accf`, `275ed4f`, `f8897e9` each record a ruling and its kill-reason. Reopening one costs a counter-example somebody measured, named against the leg it breaks.
- Commit messages carry the reasoning — what was observed, what changed, why — because the repo has no other record of its rulings.
