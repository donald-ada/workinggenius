{
  "description": "The two judges as a function hook, where function hooks are on: installed with the plugin but armed only once skill.prompt sees /enable or /tenacity in this session, never by installation; classic.Stop and classic.SubagentStop judged by the session's own model on the turn's last message alone, with every failure of the judge's own allowing the stop and handing it to the classic command hook beneath",
  "modules": ["./register.ts"]
}
